const canvas = document.querySelector('#draw')
const ctx = canvas.getContext('2d')

ctx.fillstyle = 'red'

function draw(x, y)
const circle = new Path2D();
circle.arc(0, 0, 25, 0, 2 * Math.PI);
ctx.fill(circle)

canvas.addEvenListener('nousedown',(e) => {
  const
})